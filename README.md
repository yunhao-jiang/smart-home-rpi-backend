# smart-home-rpi-backend

# To install
Use  `poetry install`

If poetry stuck at resolving dependency, run `poetry config keyring.enabled false` and then try again.

# To run
Use `poetry run start`


